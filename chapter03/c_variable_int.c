#include <stdio.h>

/**
 *  자료형(data type)
 *      + 크게 정수형, 실수형으로 구분
 *      + 정수형: char(8비트), short[16비트], int[32비트], long[32비트], long long[64비트] →순서대로 커진다
 *        (무슨자료형이있고 어떤 순서대로 커진다 외워야함)
 *      + 대부분 int(4 Byte[바이트]) 사용
 *      + sizeof() 함수 사용하면 자료형의 크기 확인 가능
 *      + short: -32767 ~ 32767
 *      + int: -2147483647~2147483647
 *
 * 
 *  컴퓨터가 가격
 *  + 수학점수 75점
 *  + int math = 75;
 * 
*/

int main(void)
{
    return 0;
}
